#!/usr/bin/env bash

CMD="$1"

# Skip Claude commands - they have dedicated notification hooks
[[ "$CMD" =~ ^claude ]] && exit 0

# Send notification without "Click to focus" action text
dunstify -a "kitty" -t "5000" " Finished" "$CMD"
