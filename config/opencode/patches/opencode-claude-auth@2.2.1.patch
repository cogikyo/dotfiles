diff --git a/node_modules/opencode-claude-auth/.bun-tag-5dac90dbd2fe4ac9 b/.bun-tag-5dac90dbd2fe4ac9
new file mode 100644
index 0000000000000000000000000000000000000000..e69de29bb2d1d6434b8b29ae775ad8c2e48c5391
diff --git a/dist/betas.js b/dist/betas.js
index 5a5c86db8dee59e1dbe516b3ba01897f0167fb17..37a68e2f76ec6dadcd4f6a30f7de5df04bd8e642 100644
--- a/dist/betas.js
+++ b/dist/betas.js
@@ -8,11 +8,12 @@ function getRequiredBetas() {
         .filter(Boolean);
 }
 // Session-level cache of excluded beta flags per model (resets on process restart)
+export function createBetaState() {
 const excludedBetas = new Map();
 // Track the last-seen beta flags env var and model to detect changes
 let lastBetaFlagsEnv = process.env.ANTHROPIC_BETA_FLAGS;
 let lastModelId;
-export function getExcludedBetas(modelId) {
+function getExcludedBetas(modelId) {
     // Reset exclusions if user changed ANTHROPIC_BETA_FLAGS
     const currentBetaFlags = process.env.ANTHROPIC_BETA_FLAGS;
     if (currentBetaFlags !== lastBetaFlagsEnv) {
@@ -26,21 +27,21 @@ export function getExcludedBetas(modelId) {
     lastModelId = modelId;
     return excludedBetas.get(modelId) ?? new Set();
 }
-export function addExcludedBeta(modelId, beta) {
+function addExcludedBeta(modelId, beta) {
     const existing = excludedBetas.get(modelId) ?? new Set();
     existing.add(beta);
     excludedBetas.set(modelId, existing);
 }
-export function resetExcludedBetas() {
+function resetExcludedBetas() {
     excludedBetas.clear();
     lastModelId = undefined;
 }
-export function isLongContextError(responseBody) {
+function isLongContextError(responseBody) {
     return (responseBody.includes("Extra usage is required for long context requests") ||
         responseBody.includes("long context beta is not yet available") ||
         responseBody.includes("You're out of extra usage"));
 }
-export function getNextBetaToExclude(modelId) {
+function getNextBetaToExclude(modelId) {
     const excluded = getExcludedBetas(modelId);
     for (const beta of LONG_CONTEXT_BETAS) {
         if (!excluded.has(beta)) {
@@ -49,6 +50,9 @@ export function getNextBetaToExclude(modelId) {
     }
     return null; // All long-context betas already excluded
 }
+return { getExcludedBetas, addExcludedBeta, resetExcludedBetas, getNextBetaToExclude, isLongContextError };
+}
+export const { getExcludedBetas, addExcludedBeta, resetExcludedBetas, getNextBetaToExclude, isLongContextError } = createBetaState();
 export function getModelBetas(modelId, excluded) {
     let betas = [...getRequiredBetas()];
     // The legacy context-1m-2025-08-07 beta is never sent — the API supports
diff --git a/dist/credentials.js b/dist/credentials.js
index 68c36378aea9674de0d375b32dc51cb037c1d9b0..60e8b7c335b2d5bd010575b173f2160bba81d8cc 100644
--- a/dist/credentials.js
+++ b/dist/credentials.js
@@ -8,6 +8,7 @@ import { fetchWithRetry } from "./http.js";
 import { log } from "./logger.js";
 import { classifyRefreshFailure, clearRefreshOutcome, getRefreshCooldownUntil, getRefreshFailureKind, isRefreshCooldownActive, noteRefreshTerminal, noteRefreshTransient, } from "./refresh-backoff.js";
 import { acquireRefreshLock } from "./refresh-lock.js";
+export function createCredentials({ readAccounts = readAllClaudeAccounts, syncAuth, resetBetas = resetExcludedBetas, fixed = false } = {}) {
 const CREDENTIAL_CACHE_TTL_MS = 30_000;
 // Only inside this window will the claude CLI actually rotate a token, so
 // it is also the only window where spawning it is worth a real API request.
@@ -20,20 +21,20 @@ const inFlightRefreshes = new Map();
 const borrowedCredentialAccounts = new WeakSet();
 let activeAccountSource = null;
 let allAccounts = [];
-export function initAccounts(accounts) {
+function initAccounts(accounts) {
     allAccounts = accounts;
 }
-export function setActiveAccountSource(source) {
+function setActiveAccountSource(source) {
     const previous = activeAccountSource;
     activeAccountSource = source;
     accountCacheMap.delete(source);
-    resetExcludedBetas();
+    resetBetas();
     if (previous && previous !== source) {
         log("account_switch", { newSource: source, previousSource: previous });
     }
 }
-export function refreshAccountsList() {
-    const fresh = readAllClaudeAccounts();
+function refreshAccountsList() {
+    const fresh = readAccounts();
     if (fresh.length === 0 && allAccounts.length > 0) {
         // Transient empty read (e.g. keychain race while the claude CLI rewrites
         // credentials) must not clobber a working session.
@@ -43,7 +44,9 @@ export function refreshAccountsList() {
     allAccounts = fresh;
     return allAccounts;
 }
-export function getActiveAccount() {
+function getActiveAccount() {
+    if (fixed && allAccounts.length === 0)
+        allAccounts = readAccounts();
     if (allAccounts.length === 0)
         return null;
     if (activeAccountSource) {
@@ -56,7 +59,8 @@ export function getActiveAccount() {
 function getAccountStateFile() {
     return join(homedir(), ".local", "share", "opencode", "claude-account-source.txt");
 }
-export function loadPersistedAccountSource() {
+function loadPersistedAccountSource() {
+    if (fixed) return null;
     try {
         const path = getAccountStateFile();
         if (existsSync(path)) {
@@ -68,7 +72,8 @@ export function loadPersistedAccountSource() {
     }
     return null;
 }
-export function saveAccountSource(source) {
+function saveAccountSource(source) {
+    if (fixed) return;
     try {
         const path = getAccountStateFile();
         const dir = dirname(path);
@@ -120,7 +125,8 @@ function syncToPath(authPath, creds) {
         chmodSync(authPath, 0o600);
     }
 }
-export function syncAuthJson(creds) {
+function syncAuthJson(creds) {
+    if (syncAuth) return syncAuth(creds);
     for (const authPath of getAuthJsonPaths()) {
         try {
             syncToPath(authPath, creds);
@@ -136,9 +142,9 @@ export function syncAuthJson(creds) {
         }
     }
 }
-export const OAUTH_TOKEN_URL = "https://claude.ai/v1/oauth/token";
-export const OAUTH_CLIENT_ID = "9d1c250a-e61b-44d9-88ed-5944d1962f5e";
-export function parseOAuthResponse(raw, currentRefreshToken, now = Date.now()) {
+const OAUTH_TOKEN_URL = "https://claude.ai/v1/oauth/token";
+const OAUTH_CLIENT_ID = "9d1c250a-e61b-44d9-88ed-5944d1962f5e";
+function parseOAuthResponse(raw, currentRefreshToken, now = Date.now()) {
     let data;
     try {
         data = JSON.parse(raw);
@@ -168,7 +174,7 @@ export function parseOAuthResponse(raw, currentRefreshToken, now = Date.now()) {
  * envelope (`{ error: { type, message } }`). Values are truncated and never
  * include tokens; the logger additionally redacts anything JWT-shaped.
  */
-export function extractOAuthError(raw) {
+function extractOAuthError(raw) {
     let data;
     try {
         data = JSON.parse(raw);
@@ -213,7 +219,7 @@ function parseRetryAfterMs(headerValue) {
  * Exchange a refresh token for fresh credentials and classify the result.
  * See {@link RefreshOutcome}. Uses the runtime's own fetch (no subprocess).
  */
-export async function refreshViaOAuthDetailed(refreshToken, timeoutMs = OAUTH_TIMEOUT_MS) {
+async function refreshViaOAuthDetailed(refreshToken, timeoutMs = OAUTH_TIMEOUT_MS) {
     const body = new URLSearchParams({
         grant_type: "refresh_token",
         client_id: OAUTH_CLIENT_ID,
@@ -283,7 +289,7 @@ export async function refreshViaOAuthDetailed(refreshToken, timeoutMs = OAUTH_TI
  * Prefer {@link refreshViaOAuthDetailed} when the transient/terminal
  * distinction matters (cooldown, CLI-fallback gating).
  */
-export async function refreshViaOAuth(refreshToken, timeoutMs = OAUTH_TIMEOUT_MS) {
+async function refreshViaOAuth(refreshToken, timeoutMs = OAUTH_TIMEOUT_MS) {
     const outcome = await refreshViaOAuthDetailed(refreshToken, timeoutMs);
     return outcome.kind === "ok" ? outcome.creds : null;
 }
@@ -334,7 +340,7 @@ function refreshViaCli(configDir, requireConfigDir = false) {
  * of threshold, so this always operates on the currently active account
  * unless one is explicitly passed in.
  */
-export async function refreshIfNeeded(account, thresholdMs = 60_000) {
+async function refreshIfNeeded(account, thresholdMs = 60_000) {
     const target = account ?? getActiveAccount();
     if (!target)
         return null;
@@ -603,6 +609,7 @@ async function performRefresh(target, creds) {
             return stored;
         }
     }
+    if (fixed) return null;
     log("refresh_fallback_cli", { source: target.source });
     const isSuffixedAccount = target.source !== PRIMARY_SERVICE &&
         target.source.startsWith(PRIMARY_SERVICE + "-");
@@ -697,6 +704,7 @@ async function refreshBorrowedAccount(target) {
     return null;
 }
 function tryFallbackAccount(excludeSource) {
+    if (fixed) return null;
     const now = Date.now();
     const candidates = allAccounts.filter((a) => a.source !== excludeSource);
     // Accounts whose in-memory credentials are still valid can be borrowed
@@ -733,7 +741,7 @@ function tryFallbackAccount(excludeSource) {
     }
     return null;
 }
-export function getCredentialsForSync() {
+function getCredentialsForSync() {
     const account = getActiveAccount();
     if (!account)
         return null;
@@ -755,7 +763,7 @@ export function getCredentialsForSync() {
  * follow-up; until then it must stay consistent with the read paths that
  * are live, hence the configDir below.
  */
-export function reloadActiveAccount() {
+function reloadActiveAccount() {
     const account = getActiveAccount();
     if (!account)
         return;
@@ -778,7 +786,7 @@ export function reloadActiveAccount() {
  * On success the account, its source, and the cache are all updated.
  * The refresh function is injectable for tests.
  */
-export async function forceRefreshActiveAccount(refresh = refreshViaOAuth) {
+async function forceRefreshActiveAccount(refresh = refreshViaOAuth) {
     const account = getActiveAccount();
     if (!account?.credentials.refreshToken)
         return null;
@@ -817,14 +825,14 @@ export async function forceRefreshActiveAccount(refresh = refreshViaOAuth) {
  * 30s TTL. Used when the API rejects a token (401) that still looks
  * valid locally.
  */
-export function invalidateCredentialCache() {
+function invalidateCredentialCache() {
     const account = getActiveAccount();
     if (account) {
         accountCacheMap.delete(account.source);
         log("cache_invalidated", { source: account.source });
     }
 }
-export async function getCachedCredentials() {
+async function getCachedCredentials() {
     const account = getActiveAccount();
     if (!account)
         return null;
@@ -882,7 +890,7 @@ function sleepAbortable(ms, signal) {
  * failure (dead refresh token) or when the wait budget is exhausted, so the
  * caller can decide between a retryable response and a hard error.
  */
-export async function getCredentialsWithBackoff(opts = {}) {
+async function getCredentialsWithBackoff(opts = {}) {
     const first = await getCachedCredentials();
     if (first)
         return first;
@@ -922,7 +930,7 @@ export async function getCredentialsWithBackoff(opts = {}) {
  * deciding between a retryable response and a hard "re-authenticate" error.
  * An active cooldown implies a transient failure.
  */
-export function getActiveRefreshFailureKind() {
+function getActiveRefreshFailureKind() {
     const source = getActiveAccount()?.source;
     if (!source)
         return null;
@@ -931,7 +939,7 @@ export function getActiveRefreshFailureKind() {
         return "transient";
     return kind;
 }
-export function reloadCredentialsFromSource() {
+function reloadCredentialsFromSource() {
     const account = getActiveAccount();
     if (!account)
         return null;
@@ -981,4 +989,23 @@ export function reloadCredentialsFromSource() {
     });
     return reloaded;
 }
+return {
+    initAccounts, setActiveAccountSource, refreshAccountsList, getActiveAccount,
+    loadPersistedAccountSource, saveAccountSource, syncAuthJson,
+    OAUTH_TOKEN_URL, OAUTH_CLIENT_ID, parseOAuthResponse, extractOAuthError,
+    refreshViaOAuthDetailed, refreshViaOAuth, refreshIfNeeded, getCredentialsForSync,
+    reloadActiveAccount, forceRefreshActiveAccount, invalidateCredentialCache,
+    getCachedCredentials, getCredentialsWithBackoff, getActiveRefreshFailureKind,
+    reloadCredentialsFromSource,
+};
+}
+export const {
+    initAccounts, setActiveAccountSource, refreshAccountsList, getActiveAccount,
+    loadPersistedAccountSource, saveAccountSource, syncAuthJson,
+    OAUTH_TOKEN_URL, OAUTH_CLIENT_ID, parseOAuthResponse, extractOAuthError,
+    refreshViaOAuthDetailed, refreshViaOAuth, refreshIfNeeded, getCredentialsForSync,
+    reloadActiveAccount, forceRefreshActiveAccount, invalidateCredentialCache,
+    getCachedCredentials, getCredentialsWithBackoff, getActiveRefreshFailureKind,
+    reloadCredentialsFromSource,
+} = createCredentials();
 //# sourceMappingURL=credentials.js.map
\ No newline at end of file
diff --git a/dist/index.js b/dist/index.js
index af0c18608caea4a568d87ba8ae4624580f11abbb..82b183aed896f369cf91f01e4b33390f77033739 100644
--- a/dist/index.js
+++ b/dist/index.js
@@ -1,11 +1,11 @@
 import crypto from "node:crypto";
 import { config } from "./model-config.js";
-import { readAllClaudeAccounts } from "./keychain.js";
+import { readAllClaudeAccounts, readCredentialsFile } from "./keychain.js";
 import { initLogger, log } from "./logger.js";
 import { fetchWithRetry } from "./http.js";
-import { addExcludedBeta, getExcludedBetas, getModelBetas, getNextBetaToExclude, isLongContextError, LONG_CONTEXT_BETAS, } from "./betas.js";
+import { createBetaState, getModelBetas, isLongContextError, LONG_CONTEXT_BETAS, } from "./betas.js";
 import { SYSTEM_IDENTITY, transformBody, transformResponseStream, } from "./transforms.js";
-import { getCachedCredentials, getCredentialsWithBackoff, getActiveRefreshFailureKind, reloadCredentialsFromSource, forceRefreshActiveAccount, getActiveAccount, syncAuthJson, initAccounts, setActiveAccountSource, loadPersistedAccountSource, saveAccountSource, refreshAccountsList, refreshIfNeeded, } from "./credentials.js";
+import { createCredentials } from "./credentials.js";
 export { addExcludedBeta, getExcludedBetas, getModelBetas, getNextBetaToExclude, isLongContextError, LONG_CONTEXT_BETAS, } from "./betas.js";
 export { resetExcludedBetas } from "./betas.js";
 export { fetchWithRetry } from "./http.js";
@@ -99,11 +99,39 @@ export function buildRequestHeaders(input, init, accessToken, modelId = "unknown
 }
 const SYNC_INTERVAL = 5 * 60 * 1000; // 5 minutes
 const PROACTIVE_REFRESH_THRESHOLD_MS = 60 * 60 * 1000; // 1 hour before expiry
-const plugin = async () => {
+let authSync = Promise.resolve();
+const plugin = async ({ client }, options = {}) => {
+    const providerID = options.providerID ?? "anthropic";
+    const configDir = options.configDir;
+    const loginCommand = options.loginCommand ?? "claude auth login --claudeai";
+    if (providerID !== "anthropic" && !configDir)
+        throw new Error("A custom Claude provider requires configDir");
+    const readAccounts = configDir ? () => {
+        const credentials = readCredentialsFile(configDir);
+        return credentials ? [{ label: providerID, source: `file:${configDir}`, configDir, credentials }] : [];
+    } : readAllClaudeAccounts;
+    const { addExcludedBeta, getExcludedBetas, getNextBetaToExclude, resetExcludedBetas } = createBetaState();
+    const { getCachedCredentials, getCredentialsWithBackoff, getActiveRefreshFailureKind,
+        reloadCredentialsFromSource, forceRefreshActiveAccount, getActiveAccount, syncAuthJson,
+        initAccounts, setActiveAccountSource, loadPersistedAccountSource, saveAccountSource,
+        refreshAccountsList, refreshIfNeeded } = createCredentials({
+        readAccounts,
+        fixed: Boolean(configDir),
+        resetBetas: resetExcludedBetas,
+        syncAuth: async (creds) => {
+            const pending = authSync.then(() => client.auth.set({
+                path: { id: providerID },
+                body: { type: "oauth", access: creds.accessToken, refresh: creds.refreshToken, expires: creds.expiresAt },
+                throwOnError: true,
+            }));
+            authSync = pending.catch(() => {});
+            await pending;
+        },
+    });
     initLogger();
     let accounts = [];
     try {
-        accounts = readAllClaudeAccounts();
+        accounts = readAccounts();
     }
     catch (err) {
         const error = err instanceof Error ? err.message : String(err);
@@ -125,10 +153,11 @@ const plugin = async () => {
         });
         const initialCreds = await getCachedCredentials();
         if (initialCreds) {
-            syncAuthJson(initialCreds);
+            await syncAuthJson(initialCreds);
         }
         else {
-            console.warn("opencode-claude-auth: Claude credentials are expired and could not be refreshed. Run `claude` to re-authenticate.");
+            if (configDir) await syncAuthJson(defaultAccount.credentials);
+            console.warn(`${providerID}: Claude credentials could not be refreshed. Run ${loginCommand}.`);
         }
         // Keep auth.json synced and proactively refresh before expiry.
         // refreshIfNeeded() always resolves the currently ACTIVE account
@@ -150,7 +179,7 @@ const plugin = async () => {
                 });
                 const creds = await refreshIfNeeded(undefined, PROACTIVE_REFRESH_THRESHOLD_MS);
                 if (creds) {
-                    syncAuthJson(creds);
+                    await syncAuthJson(creds);
                     if (proactiveRefreshWarned) {
                         log("proactive_refresh_recovered", { source: account?.source });
                     }
@@ -162,7 +191,7 @@ const plugin = async () => {
                     // SYNC_INTERVAL (5 min) for as long as refresh keeps failing.
                     if (!proactiveRefreshWarned) {
                         proactiveRefreshWarned = true;
-                        console.warn("opencode-claude-auth: Proactive token refresh failed. Run `claude` to re-authenticate.");
+                        console.warn(`${providerID}: Proactive token refresh failed. Run ${loginCommand}.`);
                     }
                 }
             }
@@ -174,11 +203,16 @@ const plugin = async () => {
     }
     else {
         log("plugin_init_no_accounts", { reason: "no credentials found" });
-        console.warn("opencode-claude-auth: No Claude Code credentials found. Running in API key mode with transform hook enabled.");
+        if (configDir) {
+            await syncAuthJson({ accessToken: "", refreshToken: "", expiresAt: 0 });
+            console.warn(`${providerID}: No credentials found. Run ${loginCommand}.`);
+        } else {
+            console.warn("opencode-claude-auth: No Claude Code credentials found. Running in API key mode with transform hook enabled.");
+        }
     }
     return {
         "experimental.chat.system.transform": async (input, output) => {
-            if (input.model?.providerID !== "anthropic") {
+            if (input.model?.providerID !== providerID) {
                 return;
             }
             const hasIdentityPrefix = output.system.some((entry) => entry.includes(SYSTEM_IDENTITY));
@@ -187,11 +221,11 @@ const plugin = async () => {
             }
         },
         auth: {
-            provider: "anthropic",
+            provider: providerID,
             async loader(getAuth, provider) {
                 const auth = await getAuth();
                 log("auth_loader_called", { authType: auth.type });
-                if (auth.type !== "oauth") {
+                if (!configDir && auth.type !== "oauth") {
                     log("auth_loader_skipped", {
                         authType: auth.type,
                         reason: "auth type is not oauth",
@@ -246,7 +280,7 @@ const plugin = async () => {
                                 });
                             }
                             log("fetch_no_credentials", { modelId: "unknown" });
-                            throw new Error("Claude Code credentials are unavailable or expired. Run `claude` to refresh them.");
+                            throw new Error(`${providerID}: Claude credentials are unavailable or expired. Run ${loginCommand}.`);
                         }
                         const bodyStr = typeof requestInit.body === "string"
                             ? requestInit.body
@@ -515,16 +549,17 @@ const plugin = async () => {
                     },
                     async authorize(inputs) {
                         const latestAccounts = refreshAccountsList();
-                        const source = inputs?.account ?? latestAccounts[0]?.source ?? accounts[0].source;
+                        const source = inputs?.account ?? latestAccounts[0]?.source ?? accounts[0]?.source;
                         const chosen = latestAccounts.find((a) => a.source === source) ??
                             accounts.find((a) => a.source === source) ??
                             latestAccounts[0] ??
                             accounts[0];
+                        if (!chosen) throw new Error(`${providerID}: No credentials found. Run ${loginCommand}.`);
                         setActiveAccountSource(chosen.source);
                         const creds = (await getCachedCredentials()) ?? chosen.credentials;
-                        syncAuthJson(creds);
+                        await syncAuthJson(creds);
                         saveAccountSource(chosen.source);
-                        const sourceDescription = chosen.source === "file"
+                        const sourceDescription = chosen.source === "file" || chosen.source.startsWith("file:")
                             ? `credentials file (${chosen.configDir ?? "~/.claude"}/.credentials.json)`
                             : `macOS Keychain (${chosen.source})`;
                         return {
@@ -534,7 +569,7 @@ const plugin = async () => {
                             async callback() {
                                 return {
                                     type: "success",
-                                    provider: "anthropic",
+                                    provider: providerID,
                                     access: creds.accessToken,
                                     refresh: creds.refreshToken,
                                     expires: creds.expiresAt,
diff --git a/dist/keychain.js b/dist/keychain.js
index 2168df2cc08a9d583fdad7f9fd4bc36feace1ba4..76cc85a18c3a1e748fec0d9d495f79b75d05c8ce 100644
--- a/dist/keychain.js
+++ b/dist/keychain.js
@@ -376,7 +376,7 @@ export function writeBackCredentials(source, creds, configDir, expectedPriorAcce
         refreshToken: creds.refreshToken,
         expiresAt: creds.expiresAt,
     };
-    if (source === "file") {
+    if (source === "file" || source.startsWith("file:")) {
         try {
             const dir = configDir ?? process.env.CLAUDE_CONFIG_DIR ?? join(homedir(), ".claude");
             const credPath = join(dir, ".credentials.json");
@@ -461,7 +461,7 @@ export function writeBackCredentials(source, creds, configDir, expectedPriorAcce
     return false;
 }
 export function refreshAccount(source, configDir) {
-    if (source === "file") {
+    if (source === "file" || source.startsWith("file:")) {
         return readCredentialsFile(configDir);
     }
     const raw = readKeychainService(source);
