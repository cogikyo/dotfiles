---
name: prose
description: Use when writing or revising human-facing prose; extends global style with audience, evidence, and editing procedure and points to documentation and attention-friendly subskills.
---

# Prose

The global `AGENTS.md` owns universal prose style.
This procedure is available to any relevant owner and does not change that owner's write scope or authority.

## Compose

Read the relevant subskills:

- `prose-docs` (`prose/docs/SKILL.md`) for READMEs, guides, usage notes, and durable repository documentation.
- `adhd` (`prose/adhd/SKILL.md`) when the task calls for attention-friendly prose or lower reading and decision overhead.

These can be combined; attention-friendly presentation must preserve a document's necessary explanation and safety context.
The existing catalog name `docs` belongs to `diagrams/docs/SKILL.md`; load it only for an already-chosen diagram or annotated tree.

## Procedure

Identify the intended reader, their task or question, and the source that supports the answer.
Inspect relevant code, config, commands, or supplied evidence before making technical claims; neighboring writing can inform vocabulary and voice but cannot override source truth.
Verify exact names, options, constraints, and examples, and expose missing evidence instead of filling it with expectations.

Choose content and structure for the reader's need, then remove repetition before polishing sentences.
Keep a qualification with the claim it limits, and preserve a causal explanation when removing it would invite a wrong inference.
Read the result as a new reader: can they understand the claim, make the intended decision, or complete the task without guessing?
Report source conflicts and unverified claims rather than hiding them with confident wording.
