---
description: Diagnose failed commands and permission denials in recent OpenCode sessions.
---

Load the `papercuts` skill and follow it.
Classify denied or asked bash in the live OpenCode log.
Stay read-only unless the user already asked to apply a fix.

Window: $ARGUMENTS
