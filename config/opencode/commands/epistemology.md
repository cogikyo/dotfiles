---
description: Mine OpenCode sessions for evidence-backed skill and instruction improvements.
---

Load the `epistemology` skill and follow it.
Mine the live OpenCode session store and return proposals only.
Stay read-only unless the user already asked to apply a change.

Scope: $ARGUMENTS
